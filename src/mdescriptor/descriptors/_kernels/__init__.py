"""Private numerical kernels retained behind the public descriptor boundary.

Nothing in this package is part of the public API. New callers must use
``mdescriptor.descriptors`` and the shared contracts in ``mdescriptor.core``.
"""
